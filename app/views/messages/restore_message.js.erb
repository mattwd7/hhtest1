$("#<%= params[:d_id] %>").remove();
<% if @trashed_messages.count == 0 %>
	$("div #trashed_messages").empty()
	$("div #trashed_messages").append($("<div class='empty_statement'>Your trash is empty!</div>"))
<% end %>
$("div #new_messages").empty();
$("div #new_messages").append("<%= escape_javascript(render :partial => 'new_messages') %>")